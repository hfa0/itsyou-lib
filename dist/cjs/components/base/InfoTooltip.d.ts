import { ReactNode } from 'react';
interface Props {
    children: ReactNode;
    size?: number;
}
declare function InfoTooltip({ children, size }: Props): import("react/jsx-runtime").JSX.Element;
export default InfoTooltip;
